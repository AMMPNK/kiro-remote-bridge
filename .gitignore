# 构建产物。scripts/package.js 只覆盖「当前版本」那一个 vsix，历史版本会留在这里
# 累积（便于回装旧版本），并不会整体重建 —— 别在这里放需要留存的东西：
# 每个 vsix 都能从对应的 git tag 重新打出来，所以这个目录随时可以整个删掉。
dist/

# 测试用的参考实现（可选开发依赖，按需 npm i）
test/ref/node_modules/
test/ref/package-lock.json

.DS_Store
