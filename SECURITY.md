# 安全说明与自查记录

这份文档有两个用途：告诉你**报告漏洞的方式**，以及诚实交代**已经查过什么、没查过什么**。

## 报告漏洞

发现问题请**不要开公开 issue**，直接在 GitHub 上给仓库作者发私信，或开一个
[Security Advisory](https://docs.github.com/en/code-security/security-advisories)。

我不是安全从业者，也没有响应时限承诺。如果问题严重而我没能及时处理，
**建议你直接在 README 里加一条警告的 PR，或者 fork 出去自己修** ——
让使用者尽快知道比等我修更重要。

## 先说清楚这东西的威胁模型

**持有访问 URL（含 token）= 能让运行 Kiro 的那台机器执行命令。**

不是"能看会话"，是"能执行"。因为手机端可以给 agent 发消息，而 agent 具备写文件和
执行命令的能力；能批准工具调用意味着连人工确认这道闸门也在远端。
所以 token 的安全等级应当按**该机器的 shell 访问权**来对待。

有多大破坏力取决于你的 `~/.kiro/settings/permissions.yaml` 放开了多少 ——
放开得越多，token 泄露的后果越大。

## 已知的设计取舍（不是 bug，但你得知道）

| 事实 | 影响 | 为什么这么做 |
|---|---|---|
| **HTTP 明文，没有 TLS** | 同网段可嗅探 token 与全部会话内容 | 局域网自签证书会让手机每次都跳警告，扫码即用就没了。代价是**不能在公共 WiFi 用** |
| token 出现在 URL 的 query 里 | 会进浏览器历史；分享链接等于分享权限 | 扫码是唯一能免输入的入口。已设 `referrer-policy: no-referrer` 防外泄 |
| 默认监听 `0.0.0.0` | 同局域网任意设备可达该端口 | 手机要连。可用 `kiroBridge.bindLan: false` 关掉，但那样手机也连不上了 |
| 限速表被打满后整表重置 | 大量不同来源 IP 可让限速短暂失效 | 宁可放宽限速也不让内存无上界。token 有 256 位熵，暴力破解不是现实威胁 |
| `/api/auth` 可探测 token 是否有效 | 攻击者能确认自己猜的对不对 | 前端需要区分「token 失效」和「网络不通」。有限速 + 256 位熵兜底 |
| 未回应的授权请求永不过期 | 一个很久前的授权框仍可被批准 | 与桌面端行为一致。实测有等待 607 分钟后正常批准的情况 |

## 自查过什么（2026-08，代码级，非专业审计）

**结论：查出一处结构性隐患，已修；其余检查项通过。**

已修：
- **`esc()` 原先不转义引号**，而有 7 处属性位置用的是它（`data-id="${esc(x)}"`）。
  那些值当时都不是外部可控的自由文本（会话 id、时间戳、模型 id、base64 缩略图），
  所以不可利用；但它依赖「上游数据格式永远不含引号」这个假设。
  修法是让 `esc()` 默认转义引号 —— 结构上不可能用错，比立规矩「属性位置请用 escAttr」可靠。
  有测试钉住（t8 的 `★ esc() 转义双引号`，注入验证过会红）。

检查通过：
- **零第三方运行时依赖** —— 没有供应链攻击面，`package.json` 的 dependencies 是空的
- **XSS**：markdown 渲染先转义再替换（顺序正确），代码块也转义；
  渲染器**不生成 `<a>` 和 `<img>` 标签**，所以连 `javascript:` 协议这一类都没有攻击面
- **路径穿越**：静态文件服务用 `path.resolve` + `startsWith(mediaDir + sep)` 双重限制
- **token**：32 字节随机（256 位），比对用 `crypto.timingSafeEqual`（防时序侧信道），
  落盘权限 0600、目录 0700，**不写进日志**（只记录"已生成/已复用"）
- **拒绝服务**：WebSocket 单帧上限 8MB、权限记录上限 5000 条、限速表硬上限 4096 条，
  发送侧有背压处理 —— 都有明确上界
- **公开路径**：不需要 token 的只有 `app.html` / `qr.js` / `manifest.json` / `icon.svg`，
  都是 UI 骨架，不含会话数据（PWA 从主屏启动时 `start_url` 不带 query，
  外壳页若也要 token 会直接白屏）
- **响应头**：`x-frame-options: DENY`、`referrer-policy: no-referrer`、`cache-control: no-store`

## 没查过什么（诚实清单）

- **没有做渗透测试**，也没有用任何自动化安全扫描工具
- **没有做密码学审查** —— token 生成用的是 `crypto.randomBytes`，但整体方案没经专业评估
- **只在 macOS 验证过**。Windows / Linux 下的文件权限语义不同（0600 在 Windows 上意义有限）
- **没有审查 Kiro 自身的安全性**。本项目通过 Kiro 未公开的内部接口工作，
  那一侧的行为不在我的可控范围内
- **没有针对恶意 agent 输出做系统性 fuzz**。XSS 那条是人工读代码得出的，不是测出来的
- 没有 CSP 头。app.html 不加载任何外部资源，所以价值有限，但这仍是一项未做的加固

## 如果你要在更严肃的场景用

按风险从高到低，建议：

1. **别用公网隧道**（ngrok / Cloudflare Tunnel）。那等于把一个能执行命令的入口放到公网
2. 用完执行「Kiro Bridge: 停止远程会话」，不要长期挂着
3. 怀疑 token 泄露就执行「轮换访问 token」，旧 URL 立刻失效
4. 收紧 `~/.kiro/settings/permissions.yaml` —— 这是 token 泄露后唯一的减损手段
5. 在不可信网络（公共 WiFi、共享办公网）里不要开
