# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

> 0.1.2 及更早的版本在本文件建立之前，未逐条记录。

## [0.7.0]

**远程审批的根本原因找到了，改用正确的提交通道。** 此前所有版本都在用错的机制。

### 修复

- **权限响应改走 `_kiro/permission/respond`，不再回 JSON-RPC 应答。**

  根因在 Kiro 产物的 `MultiplexStream` 里：mux 给每个客户端标了 role，
  **observer 的 permission 应答会被直接丢弃**，日志字符串原文就是
  `discarded observer permission response ... (waiting for _kiro/permission/respond)`；
  只有 `primary`（拥有会话的桌面面板）的 JSON-RPC 应答才会转发给 agent。
  bridge 是 observer，所以此前 `connection.respond(requestId, …)` 全部被静默丢掉。

  实测后果与之完全吻合：手机点「允许」→ 电脑上的框继续挂着 → **305 秒**后以
  `cancelled` 收场、`selectedOption` 为空。而 305 秒正是产物里 `300 * 1e3` 那个超时。

  参数形状照 Kiro 自己的两处调用抄（`resolve-permission-request.ts` 与
  `supervised-mode.ts`）：`{ toolCallId, optionId, sessionId }`。
  为此在权限记账里补存了 `sessionId`（此前没存，因为旧机制不需要它）。

- 提交失败（agent 拒绝 / 超时）时如实抛错并清掉 `respondedAt`，让用户可以重试 ——
  绝不能让手机端以为批准成功了，那正是这个 bug 此前的形态。
- 缺 `sessionId` 时显式失败，而不是发一个不完整的请求。

### 为什么之前的测试没抓到

`t12-approval` 的 30 条断言全部通过，实机却无效 —— 因为它们钉的是**旧契约**：
「回一条 JSON-RPC respond，带对的 requestId，payload 形状符合 ACP 规范」。
那个形状完全正确，只是**送错了通道**。假的 mux connection 总是接受任何 respond，
所以这个验证在原理上就不可能发现「mux 会按 role 丢弃它」。

现在断言改成「走 `_kiro/permission/respond` 且带齐三个参数」，并新增一条反向断言：
**不再回 JSON-RPC 应答**。测试 266 → 271。

### 实机验证通过

手机上点 `Always allow` → **4.9 秒**后 `outcome=selected`、`selectedOption=always-accept`，
工具 `status=completed` 并返回 7254 字节真实数据。

`selectedOption` 是 `always-accept` 而不是 `accept`，说明 0.6.1 的四选项改动也生效了 ——
用户点的那个选项原样传到了 agent，没有被后端的推断降级。

同一个会话里三种结局并列，构成完整对照：

| 耗时 | 结局 | 含义 |
| --- | --- | --- |
| 0.0s | `cancelled` | 无 handler，直接返回默认值 |
| 305.2s | `cancelled` | 有 handler 在等，但收不到我们的响应 → 超时 |
| **4.9s** | **`selected`** | **修复后，手机批准落地** |

这也是「远程批准工具调用不可用」这条已知限制第一次被摘掉 —— 它从项目第一版起就在那里。

### 附带发现

产物里还有 `_kiro/userInput/respond`（形状 `{ toolCallId, action, answer }`），
对应 `user_input` 类型的交互 —— 就是「Build a Feature / Fix a Bug」那种选择题。
手机端目前只把它渲染成待确认卡片、不能回答。这条路现在是通的，但本版没做。

## [0.6.1]

一次实机测试的直接产物。手机上弹出了授权框、点了「允许」、toast 说已发出，
**但电脑上那个框仍然挂着** —— 也就是响应发出去了却没生效。

### 修复

- **授权框的按钮改为按 agent 送来的 options 动态生成。** 此前写死「允许 / 拒绝」两个，
  由后端用正则从四个选项里推断该发哪个 optionId。这有两个问题：用户无法知道自己批的
  是单次还是永久（`allow_once` 与 `allow_always` 的作用范围差别很大），而且排查时
  分不清「发错了选项」和「发对了但 agent 没接受」。

  现在四个选项（Allow / Always allow / Deny / Always deny）逐个显示成按钮，标签用
  agent 给的原文，两列网格排布；点哪个就把那个 `optionId` 原样回传。
  `session:approve` 增加 `optionId` 字段，后端只接受确实存在于本次请求里的 optionId，
  笔误会被拒绝而不是照发。不给 `optionId` 时仍退回原来的推断路径（兼容老外壳页）。

- **toast 不再让人误以为已经生效。** 原文案是「批准已发出」，实测被理解成「已提交审批」。
  现在显示实际发出的 optionId 并明确标注还在等 agent 确认：
  `已发出：always-accept · 等 agent 确认`。

### 可观测性

- 发出响应时把 `requestId` 与完整 payload 记进日志。远程批准不生效时，第一件要核对的
  就是这个 —— 此前日志只有 optionId，看不出发给了哪个 requestId、payload 形状对不对。

### 说明

这一版让「手机上能精确选择四个选项之一」成立，也把诊断信息补齐了。但**响应为什么
没被 agent 接受，目前仍未定位** —— 需要下一次实机测试的日志才能判断是 requestId 对不上、
payload 形状不符、还是 agent 只接受拥有该会话的那个客户端的响应。

## [0.6.0]

目标是让审批的行为**与电脑端一致**：有审批就一直等你，不自己作废；等审批的会话在列表上
显示成琥珀色「待确认」并排到最前。

### 变更

- **未回应的权限请求不再过期。** 0.5.0 用了 30 分钟 TTL、0.5.1 放宽到 24 小时 —— 两版
  都是同一个毛病，只是把门槛抬高了：拿时钟去猜「请求还活不活」，而这件事有确定性信号
  （`interaction_resolved`），根本不需要猜。现在时间只用来回收**已经有结局的**记录
  （结局产生 10 分钟后），未回应的一律留着，等多久都能批。
- **「等确认」的判定去掉时间窗。** `liveState` 原来有一句「文件静置超过 30 分钟就直接判
  idle」的短路，为了省文件读取。它错在最要紧的场合：**agent 在等你审批时，文件本来就
  不动**。实测审批可以等 607 分钟，于是一个正等着你的会话在列表上显示成灰色 idle，
  恰好把最需要被看见的状态藏了起来。

  保留的是**字节窗口**（只读尾部 16KB）而不是时间窗口，这个区分是关键：真在等审批时
  pending 就是最后写入的事件，一定落在尾部窗口里；而如果 agent 没等它、继续跑了
  （实测有一条这样的历史记录，pending 之后又写了 1.28MB），那个 pending 会被推到窗口外，
  不会被误判成「正在等你」。去掉短路的代价实测可忽略：35 个会话全部真读 16KB 合计 3.16ms。

  前端 `liveOf` 的降级路径同样去掉了 30 分钟窗口。

  效果：等审批的会话现在显示琥珀色「待确认」徽标 + 琥珀色左侧色条，并排到列表最前
  （这些样式一直都在，只是被时间窗挡住了）。

### 新增

- `t12-approval` —— 审批链路的端到端验证，19 条。这是本项目最长期悬而未决的一件事，
  此前没有任何自动化覆盖。用的选项形状照真实数据抄（本机 703 个选项都是
  `{optionId, name, kind}`），覆盖：记账、从真实选项里认出 allow/deny、
  **allow 取单次放行而非永久放行**、批准/拒绝的响应形状、认不出 allow 时如实回 error
  而不是空的 selected、挂 400 天照样能批、结局对账、已被取消时说清楚而不假装成功、
  以及任何失败都不退化成 `runOrAcceptAll` 整批批准。

### 说明

桥这一侧的审批链路现在有测试兜住了。**仍然没有验证的是最后一段**：手机发出的响应到
agent 之后，agent 是否真的执行。这一段要靠实机跑一次，日志里会打印

```
[approve] ★ 远程批准结果 toolCallId=… 手机请求=allow 实际结局=selected 间隔=…ms → 落地了
```

## [0.5.1]

起因是一次实际使用：手机上看到一条「待确认」，四个选项显示成空的「 /  /  / 」，也批不动。
翻本机 183 次 `tool_approval` 的真实数据之后，发现三件事，其中一件是 0.5.0 自己引入的。

### 修复

- **撤掉 0.5.0 里 30 分钟的权限 TTL，改成 24 小时。** 这是 0.5.0 引入的回归。实测 183 次
  工具授权里有 10 次（5.5%）超过 30 分钟才被响应，最长 **607 分钟**，而它们**全部**是正常
  批准的。30 分钟的 TTL 会把这些合法的慢响应判成「已过期」并拒绝发出 —— 用一个新的失败
  模式换掉了旧的。TTL 的用途只是给内存兜底，不该承担「判断请求还活不活」。
- **改由真实结局判断请求是否还有效。** 文件里出现 `interaction_resolved` 就说明该请求
  已经有结局（通常是人在电脑上批的），这是权威信号，比拿时钟猜准。此后手机再点会得到
  准确原因：已被处理 / 已被取消 / 超过 24 小时，三种都不再假装成功，也都不会退化成
  `runOrAcceptAll` 整批批准。重复点击同一请求也会被拒绝，不再往同一个 requestId 上回两次。
- **待确认卡片的选项标签不再是空的。** 两类交互的字段名不一样，实测 703 个选项用 `name`、
  5 个用 `title`：
  ```
  tool_approval  {optionId, name, kind}     ← 工具授权，没有 title
  user_input     {title, description, ...}  ← 让你选方向的提问
  ```
  前端只读 `title`，于是所有工具授权卡片的选项都显示成「 /  /  / 」，而那正是最需要看清
  有哪些选项的场合。现在两个字段都认。

### 新增

- **远程批准是否落地，现在能查了。** 手机批准之后一旦等到该请求的真实结局，日志里会打印
  `[approve] ★ 远程批准结果 … 手机请求=allow 实际结局=selected 间隔=…ms → 落地了`。
  此前桥只记录「已发出」，发出之后发生了什么没人看 —— 这也是 README 那条结论下得过早的
  直接原因。

### 文档

- **改正「远程批准不生效」这条已知限制。** 原文说请求会在 6–130ms 内被自动取消，据此断言
  远程批准不生效。核对全部 183 次之后：被瞬间取消的只有 10 次（5.5%），其余 173 次一直
  活着等人响应，中位 9.5 秒。当初那 8 个样本恰好都落在被取消的那一类里。
  现在如实写成「尚未确认可用」：请求通常没死，但手机发出的响应能不能落地仍无直接证据，
  并给出用日志确认的方法。

### 工程

- 补上一个缺失方向的门禁：解析器产出的 14 种渲染 kind，前端 `nodeFor` 只处理 10 种，
  剩下 4 种被 `default: return ''` 静默丢掉，而此前只有反方向（后端广播 ⊆ 前端处理）有
  门禁。现在要么被渲染、要么在登记表里写明不渲染的理由，且登记表不允许残留已消失的 kind。
- `t1-store` 的 kind 清单改从 `sessionStore` 源码提取。0.5.0 里那份是手写的，而内容照
  「当时恰好最新的那个会话」的实际取值列 —— 换一个会话就冒出 `usage / sessionEvent /
  turnStart` 三个漏项。样本不是清单的来源，产出方才是。

## [0.5.0]

一次代码审查之后的集中修复。审查发现的问题按「安全 → 功能缺口 → 资源正确性 → 测试
工程 → 性能」推进，测试从 140 条涨到 221 条。

### 安全

- **扩展在你执行「启动远程会话」之前不再连接 agent、不再写任何文件。** 此前激活后 5 秒
  会无条件跑一遍只读诊断：连上全部 agent mux、发 4 个 RPC、把 mux token 落盘。而 mux
  端点是全窗口共享的，N 个窗口会各自连上全部 N 个端点。现在收在
  `kiroBridge.debugProbeOnStartup` 开关下，默认关闭；同样的事 `Kiro Bridge: 探测 agent
  方法` 命令随时能做一次。
- **`~/.kiro-bridge/` 收成 0700，里面所有文件收成 0600。** 此前只有 token 与 mux 端点是
  0600，而 `agent-probe.json`（36 条会话标题 + 工作区绝对路径）和 `diagnostics.json`
  落在 0644 —— 同一目录、同等敏感度，保护程度却取决于有没有人在调用处想起这件事。
  升级上来的装机会被补一次 chmod。
- 删掉 WebSocket 上的 `diagnose` handler。前端从不调用它，而它会把 store 根路径、会话
  标题、mux 端口、局域网地址整份返回给手机端。同样的东西命令面板里看得到。

### 新增

- **手机上能停掉正在跑的回合。** 正在跑时发送键换成停止键，走 `session/cancel`。
  后端一直支持这个请求，只是此前没有任何入口能发出它 —— 而一致性门禁的豁免表里把这件事
  登记成了「前端从不发这个请求」，等于把功能缺口写成了豁免。

### 修复

- **两台手机不再互抢。** 关注的会话此前是一个全局单值，第二台手机打开另一个会话就把
  第一台的 tail 抢走了 —— 第一台从此静默停止更新，界面上没有任何提示。现在记在连接上，
  会话级推送只投给在看该会话的连接（此前是广播给所有客户端再由前端自己丢掉）。
- **列表里的「运行中」徽标不再卡住。** `live` 此前不在会话列表签名里，而它是后端按时间窗
  算出来的，会在文件完全没动的情况下自己从 running 翻成 idle —— 那次翻转过去永远不会
  被渲染。
- **过期的权限请求不再假装批准成功。** 实测有相当一部分权限请求会在 6~130ms 内被 agent
  自己取消，而记账只在手机端回应时才删除。对一个早已死掉的 requestId 调 respond，agent
  那边没人在等，手机上却显示「已批准」。现在超过 30 分钟的记录显式失败，并且刻意不退回
  整批命令路径 —— `runOrAcceptAll` 的作用范围远大于用户以为自己在批准的那一个工具调用。
- **附件预算不再是一份写死的副本。** 服务端在握手时告知真实帧上限，手机端据此计算
  （数值不变，仍是 6 MB）。此前 `MAX_TOTAL_B64` 与 wsServer 的 `MAX_PAYLOAD` 只靠注释
  关联，改一边另一边会静默失配。
- 失败记录表（限速用）与权限记账都加了上界，不再只增不减。
- socket 背压变得可观测（积压字节、丢帧数、已慢多久进自诊断），积压超过 512KB 后
  周期性快照不再往队列里堆。`delta` / `history` 永不丢弃 —— 游标已经推进，丢掉就永久
  少了几条消息而界面上看不出来。

### 性能

- **`readHistory` 改成只回读文件尾部。** 35 个真实会话共 107MB，全量读的耗时中位
  14.8ms、p90 39.6ms、最大 69.8ms，17/35 超过一帧，而这是同步调用、期间扩展宿主完全
  阻塞，触发它的只是手机上点一下会话。现在最大会话 42.5ms → 14.2ms，超过一帧的会话
  12/35 → 5/35。剩下几个已经在结构下限上 —— 它们的 400 条消息本身就散布在整个文件里。
  改动前后在 36 个会话 × 4 种 limit（144 组）上逐字比对一致。
- **`findSessionDir` 加缓存。** 它原来对经过的每一个目录都读一次 `session.json`，而
  tail 每 900ms 就要查一次。单次 2.202ms → 0.001ms，tail 单次 4.3ms → 0.003ms。
  刻意只加缓存、不改扫描顺序：两趟扫描看着更快，但会让「meta.id 匹配的靠前目录」
  输给「目录名匹配的靠后目录」，而同一个 session id 确实会出现在多个 workspace 目录下。

### 工程

- **零断言的测试文件不再显示成通过。** `run-all` 新增 `EMPTY` 状态：没有结果行、或结果
  行是 0/0 而又没有 SKIP 标记的，整体非零退出。`t1-store` 此前就是这个形态 —— 纯
  `console.log`、零断言，汇总里显示 `[ OK ] 0 通过 / 0 失败`，和真正通过一模一样。
  现在它有 25 条断言，并且没有真实数据时显式走 SKIP（原来会抛 TypeError 变成 FAIL）。
- 加了 GitHub Actions：push / PR 跑全量测试 + 打包校验，并复核 t1 在 CI 上确实是 SKIP。
- 一致性门禁的豁免表清空。三条豁免都不是靠补空 handler 消化的，而是各自对应一个真实
  缺陷（功能缺口 / 写死的副本 / 不该开放的接口）。
- 清掉死代码：`lastStatus`、`sessions:list` handler、`ENDPOINTS_FILE` 常量。
- 改正 `.gitignore` 里关于 `dist/` 的说法。原注释称「每次都会整体重建」，实际只覆盖当前
  版本那一个 vsix，历史版本会累积（这是有用的，能回装旧版）。

## [0.4.2]

### 变更

- **顶栏那个点改成以连接状态为主。** 此前它被 agent 忙闲状态直接覆写，于是「连着但空闲」
  和「已断开」都是灰色 —— 而连接状态恰恰是最该被一眼看见的那一项，尤其在列表页（那里
  没有别的地方能看出链路还在不在）。

  现在是一个状态函数，不再互相覆盖：

  | 状态 | 表现 |
  | --- | --- |
  | 已断开 | 红色 |
  | 连着 · 等待确认 | 琥珀脉动 |
  | 连着 · 正在跑 | 绿色脉动 |
  | 连着 · 空闲 | 绿色常亮 |
  | 尚未连上 | 灰色 |

  `showBlocker` 也接入同一个状态源，避免内部标记与点上显示的东西各说各话。

## [0.4.1]

### 修复

- **表格在窄屏上被压成一字一行。** 原因是只给 `th` 设了 `white-space:nowrap`，`td` 没设 ——
  表格只有在**固有宽度超过容器**时才会横向溢出，否则浏览器宁可把长单元格压到极窄也不触发
  滚动。改为 `table{width:max-content}` 加单元格 `nowrap`，让它真正溢出并横向滚动。

### 移除

- 撤掉 0.3.4 加的「任意文件」入口。**它的立论是错的**：复测后「排除媒体类型」与「不限类型」
  在夸克、Chrome、飞书内置浏览器里表现**完全一致** —— 差异不是 `accept` 造成的，而是浏览器
  自身对文件选择的实现不同（夸克两种都能到文件管理器，Chrome / 飞书两种都不行）。
  0.3.4 里那张对比表据此作废。附件菜单回到「图片 / 文件」两项。

## [0.4.0]

### 新增

- **块级 markdown 渲染**：表格、代码块、标题、列表、引用。此前只有行内 `**` 与反引号，
  块级语法全部以原文显示，表格尤其难读。
  - 表格外层套横向滚动容器 —— 手机屏幕上表格必然溢出。
  - 代码块单独底色 + 横向滚动，不参与自动换行。
- 助手气泡改为在 `.a` 上保存原始文本（`data-raw`），合并相邻片段时**先合原文再整体重渲染**。
  拼 HTML 会把被切在两段之间的表格或代码块渲染成两个残缺结构。

### 说明

渲染器要在**流式的半截文本**上反复运行，所以未闭合的代码块必须能安全收尾。这一条和
「先转义再拼标签」一起做了用例覆盖（表格、代码块内的表格语法、未闭合 fence、XSS、
单独的 `---` 不误判等 12 项）。

## [0.3.4]

### 新增

- 附件菜单增加「任意文件」（不限 `accept`）。

  实测下来这是个**浏览器差异,单一 accept 无法同时满足**：

  | | 排除媒体类型（「文件」） | 不限类型（「任意文件」） |
  | --- | --- | --- |
  | 夸克 | 直达文件管理器 | 退回媒体面板 |
  | Chrome / 飞书内置 | 给不出文件管理器 | 经系统面板「更多」可进文件管理 |

  网页无法控制 Android 的 intent 选择面板，所以两条路都留着由用户选，而不是替所有人赌一种。

### 说明

这类问题的「正解」是 Web Share Target（从文件管理器分享进 PWA），但它依赖 Service Worker，
而 Service Worker 要求安全上下文 —— 本项目是局域网明文 HTTP，注册不了。要走这条路得先有
TLS。

## [0.3.3]

### 修复

- **「文件」入口仍然只弹出媒体选择面板。** 上一版以为「不写 `accept` 就等于什么都能选」，
  方向正好相反：不写 `accept` 等同 `*/*`，于是相机 / 录音 / 相册都匹配上、占满了选择面板，
  文件管理被挤进「更多」。现在明确排除媒体类型，只留 `text/*` 与 `application/*` 及常见
  代码/文本扩展名，系统才会直接落到文档选择器。

## [0.3.2]

### 变更

- 附件入口改成**一个 `+` 按钮 + 弹出菜单**（图片 / 文件），取代原来并排的两个 emoji 按钮。
  图标改为项目既有的 SVG symbol 规范（`viewBox 0 0 24 24`、`stroke=currentColor`、
  `stroke-width 1.7`、圆角端点），与顶栏和归档页的图标同一套。

### 修复（测试基础设施）

- 「声明的 id 没有无用残留」出现**误报**：新增的 `<symbol>` 只被静态 HTML 里的
  `<use href="#x">` 引用，而该检查只扫脚本，于是把在用的图标报成残留 —— 顺着它改就会把
  图标删掉。现在把 HTML 里的 `href="#x"` 也算作引用。已验证放宽后仍能抓出真正的残留 id。

## [0.3.1]

### 修复

- **手机上只能选图片，选不到别的文件。** 原来只有一个不带 `accept` 的 file input，本意是
  「什么都能选」，但 Android 上多数浏览器此时会弹「相机 / 录音 / 照片和视频 / 更多」这种
  媒体优先面板 —— 文件要点进「更多」才能到文件管理器，而各家行为并不一致。
  改成两个明确入口：🖼 走 `accept="image/*"`，📎 走不限类型的 input。
- 添加附件成功时也给提示。此前只有失败才 toast，选了文件却没出现时分不清是选择器没给、
  还是代码把它丢了。

## [0.3.0]

### 新增

- **手机端可以上传文件了。** 输入区左侧的 ＋ 选文件，可多选，发送前能逐个移除。
  内容块形状取自 Kiro 产物里的 `zContentBlock`（均已核实）：
  图片 → `image`；文本类文件 → `resource.text`（在扩展侧解码）；其他二进制 → `resource.blob`。
- **图片在手机侧先缩边再编码**（最长边 1600、JPEG 0.85）。手机原图动辄 8–12MB，base64 后会
  超过 WebSocket 服务端 8MB 的帧上限，而视觉模型并不需要原图。
- 用户消息气泡下会显示「已附 N 个文件」，否则只看文字分不出附件到底传上去没有。

### 说明

- 单个非图片文件上限 5MB，一条消息里 base64 总量上限 6MB（服务端帧上限 8MB，留出协议余量）。
- **mux 不可用时带附件会直接报错**，不会退到命令通道 —— 那条通道只能传纯文本，
  悄悄丢掉附件再显示「已发送」是最坏的一种失败。

### 修复（测试基础设施）

- UI 一致性门禁「后端广播的类型前端都处理了」出现**误报**：它宽泛地抓所有 `type: 'x'`，
  于是把新写的 ACP 内容块（`image` / `resource` / `text`）当成了广播类型。收紧为只认真正
  发往手机的出口（`broadcast(…)` / `sendJson(…)` / handler 的 return 或箭头直接返回）。
  同时补上箭头返回形式，否则会漏掉 `diagnostics`。已验证三个方向：新增未处理类型能抓出、
  删掉前端 handler 能抓出、ACP 内容块不再误报。

## [0.2.1]

### 修复

- **重载或窗口重启后，从手机给旧会话发消息报 `Session '...' not found`。** agent 是新进程，
  内存里没有旧会话的 state（磁盘上文件都在）。现在识别到这个错误就先调
  `kiroAgent.viewSession` 让桌面加载一次，等 1.5 秒再重试一次 prompt。
  订阅走同一套恢复逻辑。
  没有用 ACP 的 `session/load`：它要求传 `mcpServers`，传空数组有可能让该会话丢掉 MCP
  工具，属于会静默降级的副作用；走桌面加载则用它自己的配置。

### 说明（升级到 0.1.8+ 的一次性影响）

访问 token 从「每次启动随机生成」改成持久化之后，**第一次运行新版本时会生成一个新
token 并落盘**。所以升级后手机上原有的链接会失效一次，需要重新扫码；此后重启不再变化。

## [0.2.0]

### 新增

- **正文也走实时流。** 此前只有思考是流式的，正文靠文件 tail 约 1 秒一批，两者体验割裂。
  现在同时消费 `agent_message_chunk`。
  实现要点：原始文本累积在 `data-raw` 上、每次整体重渲染 —— markdown 标记会被切在两个
  增量之间（`**粗` + `体**`），逐块追加 HTML 会渲染成字面的 `**粗体**`。

### 修复

- **手机新建的会话，审批请求会在 6~20ms 内被取消。** 根因在 ACP SDK：`requestPermission`
  按 sessionId 找 handler，**找不到就直接返回 `cancelled`**（源码注释原文
  "defaults to cancelled if no handler"），而 handler 由拥有该会话的桌面面板通过
  `onPermissionRequest(sessionId, …)` 注册。纯靠 mux 创建的会话没有面板拥有它，于是无
  handler、立即取消。
  现在新建会话后调用 `kiroAgent.viewSession` 让桌面 attach —— Kiro 自己的程序化建会话
  流程也是先 viewSession 再发 prompt。代价是桌面侧边栏会切到该会话。
  **只在新建时调用**，不在打开已有会话时调用：那会把你在电脑上正在用的会话挤掉，反而
  弄坏本来正常的审批。
- **订阅在 mux 重连后会静默失效。** 订阅是连接级的，重连或扩展重启后就没了，而手机端不
  一定会重新发 `session:open`。表现是「本来能收到审批，过一会儿又收不到」，且没有任何
  报错。现在每轮列表轮询核对一次 `{sessionId, port}`，端口变了（说明重连过）就重新订阅。

### 已知限制

打开一个「历史上由手机创建、桌面从未打开过」的会话时，审批仍可能被立即取消 —— 因为不会
为它调用 `viewSession`（避免挤掉你正在用的会话）。绕过办法：在电脑上打开该会话一次。

## [0.1.9]

### 修复

- **电脑侧发起的会话，在手机上收不到授权框，也没有流式思维链。** 根因：agent 只把
  `session/request_permission` 与 `session/update` 发给**已订阅该会话**的客户端，而
  `session:open` 只读本地文件、从不与 mux 交互，所以桥从未订阅过这些会话。手机上看到的
  「待确认」只是从会话文件读出的历史卡片，没有按钮。
  现在打开会话时会用该 sessionId 发一个只读的 `_kiro/session/history`（limit=1）请求。
  Kiro 的实现里，任何带 sessionId 的请求都会 `subscribeToSession`，并在此前未订阅时补发
  `resendPendingPermissions` / `resendPendingUserInputs` —— 所以已经挂起的审批也会被重新发来。

### 说明

此前记录的「权限请求在 6–130ms 内被自动取消」只在**桥自己创建的会话**里成立，不是普遍行为：
实测电脑侧会话的审批请求可存活 100 秒以上并正常批准。桥创建会话时的取消是另一个问题，
机制尚未查清，仍在跟踪。

## [0.1.8]

为「出门在外用」做准备。这两项不做的话，任何远程访问方案都会在第一次重启后失效。

### 新增

- 访问 token 持久化到 `~/.kiro-bridge/relay-token.json`（权限 0600）。此前 token 每次启动随机生成，而它只出现在电脑屏幕上的二维码里 —— 人在外面时只要扩展重新激活（重载窗口、Kiro 重启、睡醒后激活），就再也拿不到新地址，等于永久失联。
- 新命令 `Kiro Bridge: 轮换访问 token`，用于兜住持久 token 的泄露风险：作废旧 token 并重启中继。
- 中继运行期间调用 `caffeinate -i` 抑制空闲休眠，停止与 deactivate 时释放。**注意它阻止不了合盖休眠**，文档中已如实说明。
- README 增加「出门在外怎么用」：推荐组网（Tailscale / ZeroTier）而非公网隧道，并说明原因与前提条件。

## [0.1.7]

### 修复

- 模型清单不完整：`_kiro/config/template` 在结构上不返回模型项（内部把 `currentModelId` 传成 `undefined`，模型 select 整条被省略），导致取值静默退回「历史里用过的模型」。现在从 `session/new` 与 `session/set_config_option` 的返回里捕获全量清单并落盘缓存，取值改为三级阶梯 `agent → cache → history`，每级都标 `source` 并写日志。
- 默认模型不再自己编造，取自缓存下来的真实响应。

## [0.1.6]

### 新增

- 流式思维链：消费 mux 的 `agent_thought_chunk` 增量。此前该增量已被广播为 `muxUpdate`，但前端从未处理，收到即丢弃。
- 实时内容作为临时占位，权威内容（文件 tail）到达后替换，避免同一段思考重复出现。

### 修复

- 思考块不会自动折叠：原判据是「是否为最后一个 live 的思考块」，导致最近那块一直展开到下一次思考出现。改为「它后面是否还有内容」。
- UI 一致性门禁「后端广播的类型前端都处理了」失效：该检查原本拿一份**手写数组**当后端类型清单，新增 `muxUpdate` 时无人同步，于是静默放行。改为从 `src/extension.js` 与 `src/relay.js` 直接提取，新类型默认必须被处理；豁免项需登记理由。

## [0.1.5]

### 新增

- 思考块改为流式展开、结束后自动折叠，标签 `Thinking…` / `Thought complete`。
- 内联 Markdown 渲染：`**加粗**` 与 `` `行内代码` ``（先转义再替换，标记只可能来自 Markdown 语法）。

### 修复

- 连续助手片段的合并改用 `innerHTML`：原先用 `textContent` 会把 `<strong>` / `<code>` 标记抹平。
- 用户消息气泡提高对比度，在深色背景下可辨。

## [0.1.4]

### 新增

- 思考与正文分离：按 `operationType`（`Reasoning` / `Say`）拆成两类。二者共享 `executionId`，此前只按 `executionId` 合并会把内部推理和给用户的回答粘成一段。
- 思考默认折叠，复用工具卡片的折叠交互。

## [0.1.3]

### 修复

- `session/prompt` 不再使用 30 秒默认超时。该请求要等整个回合结束才返回，超时会把「已送达且正在处理」误报成发送失败，并因降级重发撞上 `A prompt is already in-flight`。改为只观察短时间内是否回错误。
- 权限响应在找不到 `allow` 选项时不再回一个缺 `optionId` 的 `selected`（协议上无效，agent 侧会当取消处理，而手机端显示「已回应」）。改为显式报错。
- 手机端不再对已被取消的权限请求显示有效授权框：结局到达后自动关闭并说明「该请求已被取消，批准未生效」。
